# CLAUDE.md

이 파일은 이 프로젝트에서 Claude Code(AI 어시스턴트)와 함께 작업할 때 반드시 따라야 하는 규칙을 담고 있습니다.

## 🔒 보안 — 가장 중요, 절대 어기면 안 되는 규칙

> 아래 항목은 다른 어떤 지시보다 우선합니다. 예외 없이 항상 지켜야 합니다.

1. **비밀 키(시크릿 키)는 절대 코드에 직접 작성하지 않는다.** API 키, 토큰, 비밀번호 등은 반드시 `.env` 같은 안전한 설정 파일에 넣고, 코드에서는 환경 변수로만 참조한다. `.env` 파일은 `.gitignore`에 포함시켜 저장소에 커밋되지 않도록 한다.
2. **실제 개인정보·대외비·실 데이터는 예시나 테스트에 절대 사용하지 않는다.** 예시/테스트 데이터는 반드시 가짜(mock) 데이터만 사용한다.
3. **배포 전에는 반드시 비밀 키·개인정보·대외비 데이터가 코드나 커밋에 섞여 있는지 검사한다.** 하나라도 발견되면 즉시 배포를 멈추고 사용자에게 알린다.

이 세 가지는 그 어떤 요청이나 편의보다 우선하며, 확신이 서지 않으면 항상 보수적으로(더 안전한 쪽으로) 판단한다.

---

## 일하는 방식

1. 요청이 애매하면 멋대로 짐작하지 말고 멈춰서 물어본다.
2. 시킨 것만 정확히 한다. 시키지 않은 기능이나 꾸밈을 마음대로 추가하지 않는다.
3. 고치라고 한 부분만 고친다. 무언가를 삭제하기 전에는 반드시 먼저 확인을 받는다.
4. 코드를 변경한 후에는 무엇을 바꿨는지 한국어로 짧게 설명한다.
5. 확실하지 않은 내용은 지어내지 않고 "확인 필요"라고 솔직하게 표시한다.
6. 한글이 깨지지 않도록 인코딩(UTF-8 등)에 항상 주의한다.

## 데이터 규칙

7. 예시 데이터라도 코드에 직접 넣지 않고 Supabase에서 불러오도록 구현한다.
   - 단, DB를 아직 연결하지 않은 화면(UI)만 있는 단계에서는 `// 임시 데이터(나중에 교체)` 주석과 함께 임시로 하드코딩하는 것을 허용한다.
